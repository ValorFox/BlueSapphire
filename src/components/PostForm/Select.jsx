import React, { useId } from "react";

function SelectCompo({ classname = "", options, defaultStatement, ...props }, ref) {
  return (
    <>
     
    </>
  );
}

export default React.forwardRef(SelectCompo);
